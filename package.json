{
	"name": "scss-intellisense-angular",
	"type": "module",
	"displayName": "SCSS IntelliSense for Angular",
	"description": "Advanced autocompletion, hover, and go-to-definition for SCSS in Angular projects",
	"version": "0.0.3",
	"preview": true,
	"publisher": "shanmukhateja",
	"author": {
		"name": "Surya Teja K",
		"email": "shanmukhateja@gmail.com"
	},
	"license": "MIT",
	"engines": {
		"vscode": "^1.53.0"
	},
	"icon": "icon.png",
	"homepage": "https://github.com/shanmukhateja/scss-intellisense-angular/blob/master/README.md",
	"repository": {
		"type": "git",
		"url": "https://github.com/shanmukhateja/scss-intellisense-angular"
	},
	"bugs": {
		"url": "https://github.com/shanmukhateja/scss-intellisense-angular/issues"
	},
	"keywords": [
		"scss",
		"angular",
		"autocompletion",
		"intellisense",
		"refactor"
	],
	"categories": [
		"Programming Languages"
	],
	"activationEvents": [
		"onLanguage:scss",
		"onLanguage:typescript"
	],
	"main": "./dist/client.js",
	"contributes": {
		"configuration": {
			"properties": {
				"scss.scannerDepth": {
					"type": "number",
					"default": 30,
					"description": "The maximum number of nested directories to scan."
				},
				"scss.scannerExclude": {
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"**/.git",
						"**/node_modules",
						"**/bower_components"
					],
					"description": "List of glob patterns for directories that are excluded when scanning."
				},
				"scss.scanImportedFiles": {
					"type": "boolean",
					"default": true,
					"description": "Eagerly follow @import/@use/@forward edges during the initial workspace scan, to warm the cache with files the scss glob might have missed. Does not control which symbols are suggested for a given file — that's decided by its own @use/@import chain plus scss.customProperties.scope for CSS custom properties."
				},
				"scss.showErrors": {
					"type": "boolean",
					"default": false,
					"description": "Allows to display errors."
				},
				"scss.suggestVariables": {
					"type": "boolean",
					"default": true,
					"description": "Allows prompt Variables."
				},
				"scss.suggestMixins": {
					"type": "boolean",
					"default": true,
					"description": "Allows prompt Mixins."
				},
				"scss.suggestFunctions": {
					"type": "boolean",
					"default": true,
					"description": "Allows prompt Functions."
				},
				"scss.suggestFunctionsInStringContextAfterSymbols": {
					"type": "string",
					"default": " (+-*%",
					"description": "Allows prompt Functions in String context after specified symbols."
				},
				"scss.dev.serverPort": {
					"type": "number",
					"description": "The port that SCSS IntelliSense server listens to. Can be used for attaching to the server Node process for debugging / profiling.",
					"default": -1
				},
				"scss.angular.includePaths": {
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [],
					"description": "Extra directories (workspace-relative or absolute) to search for bare-specifier @use/@import/@forward targets, in addition to angular.json's own stylePreprocessorOptions.includePaths. Entries resolving outside the current workspace folder are ignored."
				},
				"scss.angular.componentStyles": {
					"type": "boolean",
					"default": true,
					"description": "Enable hover, goto-definition and color quick-fixes for SCSS written inside an Angular component's inline `styles` (a .ts file). Parsing is lazy, per open document."
				},
				"scss.customProperties.scope": {
					"type": "string",
					"enum": [
						"workspace",
						"root-selectors"
					],
					"default": "workspace",
					"description": "Controls which CSS custom properties (--x) are suggested/resolved across files. \"workspace\": any --x declared anywhere is available everywhere (matches real CSS cascade behavior). \"root-selectors\": only --x declared inside a :root/html/body selector counts as global."
				}
			}
		}
	},
	"//": "esbuild bundles all deps into dist/; the only shipped node_modules package is vscode-css-languageservice (see esbuild.js for why it stays external).",
	"dependencies": {
		"vscode-css-languageservice": "6.3.10"
	},
	"devDependencies": {
		"@babel/parser": "8.0.4",
		"@babel/types": "8.0.4",
		"@nodelib/fs.macchiato": "3.0.0",
		"@types/color": "4.2.1",
		"@types/color-name": "2.0.0",
		"@types/mocha": "10.0.10",
		"@types/node": "26.1.1",
		"@types/sinon": "22.0.0",
		"@types/vscode": "1.53.0",
		"@vscode/vsce": "3.9.2",
		"color": "4.2.3",
		"color-name": "2.1.0",
		"esbuild": "0.28.2",
		"eslint": "9.14.0",
		"eslint-config-mrmlnc": "5.0.0",
		"fast-glob": "3.3.3",
		"mocha": "11.7.6",
		"rimraf": "6.1.3",
		"scss-symbols-parser": "2.0.1",
		"sinon": "22.0.0",
		"typescript": "5.6.3",
		"vscode-languageclient": "10.1.0",
		"vscode-languageserver": "10.1.0",
		"vscode-languageserver-textdocument": "1.0.12",
		"vscode-languageserver-types": "3.18.0",
		"vscode-test": "1.6.1",
		"vscode-uri": "3.1.0"
	},
	"scripts": {
		"clean": "rimraf out dist",
		"lint": "eslint \"src/**/*.ts\" --cache",
		"compile": "tsc",
		"bundle": "node esbuild.js",
		"bundle:production": "node esbuild.js --production",
		"test": "mocha \"out/**/*.spec.js\"",
		"test:e2e": "node ./out/unsafe/test/e2e/runTest.js",
		"build": "npm run clean && npm run lint && npm run compile && npm test && npm run bundle",
		"watch": "node esbuild.js --watch",
		"watch:tsc": "tsc --watch",
		"vscode:prepublish": "npm run clean && npm run compile && npm run bundle:production",
		"build:vsix": "vsce package --out release/"
	}
}
